# Sravan Kumar Koyyada — Portfolio (React + Vite + Tailwind)


This is a single-page, attractive, responsive frontend-only portfolio built with React (Vite) and Tailwind CSS. It uses no backend — contact form uses `mailto:`. Replace placeholder content if you want.
